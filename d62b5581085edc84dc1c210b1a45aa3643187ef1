Scope ballooned live from 'one script POST path' to the full sidebar data transport, driven turn-by-turn by the operator (live co-dev). Operator ruled the launching orchestrator TAINTED mid-session and declared himself sole authority; all orchestrator relays + main sidecar commits treated non-authoritative; built only on operator's direct words + real pre-existing code. Fan-out cut-over deferred (would blind the tracked inbox-reading sidebar before sidebar_v3 parity). Suppressed own status broadcasts (the fan-out is the leak being removed). Committed in small verified steps; ~6 explorers + 3 builders dispatched. Relay bus + pretty sidebar + fan-out cut-over handed to a fresh orchestrator.
