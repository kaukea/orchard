# Telemetry digest — 2026-07-26

**Window:** 2026-07-26 00:00–23:59 UTC | **Sessions:** 3 | **Fully compliant:** 0/3 | **Total deviation lines:** 12

## Interviews (verbatim)

=== 231b59d · 2026-07-26 12:01:42 +0200 · 📝 README catches up with the orchard transport

Exit interview — orchestrator session 2026-07-26 (bus-arc close + scope round)

Deviations:
- Operator questions went through AskUserQuestion, not bus.py ask
  (charter says ask-only). Judged: operator live in this pane and
  answering; the broker popup surface unbuilt/in-flux; bus.py retired on
  the landing branch. Worked well — but the charter and reality disagree;
  operator-interacting (gh#219) is where this reconciles.
- Bus sidecar not respawned after the operator stopped it mid-round
  (respected the stop; direct courier broadcasts sufficed). Charter
  assumes a sidecar always; no harm observed in a single-pane session.
- kauk sync NOT run at workflow end: the cutover-is-manual ruling
  (operator triggers sync+restart) dominated the routine-hygiene rule.
  Rules conflict; resolved toward the specific over the general.
- board_gh push exceeded the 2-minute foreground cap once (many issue
  writes post-close); reran backgrounded. Consider the background path
  the default after large badge batches.
- Two AskUserQuestion rounds rejected before answers: operator wanted my
  understanding of the features restated FIRST (acceptance mechanism),
  and context before choices. Lesson: lead multi-question rounds with the
  reformulated understanding, ask nothing before acceptance.

Friction:
- Housekeeper did not flip bus-transport-v2 (trigger lived only in MOOD
  pending, invisible to it) — orchestrator caught it. Standing triggers
  that close OTHER tasks should ride the sidecar, not MOOD.
- feature_name.py returned full board titles as window names (very long
  tmux window names) — cosmetic, tmux-naming's territory.

=== 2fbc3cc · 2026-07-27 01:46:52 +0200 · sidebar-empty-rows: squash merge + ingest + decision fold

Exit interview — f/sidebar-empty-rows, landscaper session e3e3aabd (round 2).

DEVIATIONS

1. Wrote to the WRONG CHECKOUT. Two sidecar edits used the main checkout's
   path instead of the worktree's, dropping the worktree prefix. Both
   reported success, so nothing surfaced until a line count disagreed with
   itself. Main's working tree was left dirty while on main. Repaired after
   verifying the diff was purely additive and entirely mine.
   LESSON: an edit reporting success proves the write landed SOMEWHERE, not
   where intended. Read back through the same relative path the shell uses.

2. Mis-encoded the operator's own words. He had ruled that a finished step
   "folds to a plain line as the next opens, keeping its place among the
   five"; I read "place" as a slot in a shared line and briefed a sower to
   put all five steps on ONE line and abbreviate them to fit. Corrected only
   when he restated it. The wrong reading also generated a fake defect
   (truncation) that then needed its own fix instruction.

3. Took "dimmer" to mean darker. He meant subdued — lighter, less saturated,
   or hue-harmonising. Applied wrongly across the whole colour model before
   he corrected it. Cost a rework of both grade-3 colour functions.

4. DRIP-FED REFINEMENTS one per turn until he stopped me: "this is going to
   go back and fourth so eithr we drop this discussion or we very accurately
   agree". The fix was to state a complete specification once and build it.
   This is the single most expensive process failure of the session.

5. Deprioritised the original requirement. Keyboard navigation — "the FIRST
   thing i asked" — was treated as an end-of-round check rather than the
   feature's point, and was found completely broken.

6. Relaunched a pane with a relative path, which silently started the wrong
   copy because the pane's working directory was not the worktree.

WHAT WENT RIGHT AND SHOULD BE REPEATED

- Diagnosing the failed acceptance check before building anything: the defect
  was not in the code but in which code was on screen.
- Verifying every sub-agent's claim by looking at the running system. Three
  reports were wrong or overstated: a "fixed" gardener identity that changed
  nothing, fixtures described as live that were tool-generated, and a green
  suite hiding a navigation feature that had never worked.
- Refusing to present done at any point where the evidence had not touched
  the running thing.

=== ccc5f6e · 2026-07-26 20:05:23 +0200 · 📐 Decision-096 addendum: courier-only, no exceptions

Exit interview — gardener, post-restart arc 2026-07-26 (cutover + check-a + Decision-096)

Deviations:
- Proposed killing the old sidebar pane + remounting in one stroke;
  operator rejected the tool call, then resolved it himself with a full
  restart. Lesson: when the operator announces a restart, the restart IS
  the fix path — don't race it with surgical pane work.
- Bloomer round ran without an operator channel of its own (no
  AskUserQuestion in subagent toolset); questions relayed verbatim
  through the gardener, one per window. Worked; is now also the
  Decision-096-correct shape (subagents own no surfaces).
- Direct orchard_topic.py posts were charter-compliant until the
  operator's courier-only ruling landed mid-session; switched to
  courier-routed asks the same turn. Charters amended in the same stroke
  so the rule is written, not remembered.

Friction:
- A courier sidecar per in-session subagent (~35k tokens each) surfaced
  as a cost + identity smell the operator escalated into Decision-096 —
  the design review came from watching the task list, not from any
  monitoring. Cheap observability pays.
- board_lint carries 7 pre-existing decision-* taxonomy reds; bloomer
  green-gate unsatisfiable this round; debt offered to operator, parked.

## Signal — instruction compliance

**Fully compliant:** 0/3 sessions

**Rules broken by session:**
- `AskUserQuestion surface vs. bus.py ask charter`: 1 session (231b59d)
- `Sidecar respawn covenant`: 1 session (231b59d)
- `Sync/restart hygiene: manual cutover vs. routine sync`: 1 session (231b59d)
- `Foreground operation timing (board_gh 2-min cap)`: 1 session (231b59d)
- `Multi-question acceptance flow`: 1 session (231b59d)
- `Path handling: worktree vs. main checkout`: 1 session (2fbc3cc)
- `Operator language parsing`: 1 session (2fbc3cc)
- `Specification clarity: drip-fed vs. full spec`: 1 session (2fbc3cc)
- `Requirement priority: keyboard navigation as core`: 1 session (2fbc3cc)
- `Working directory isolation`: 1 session (2fbc3cc)
- `Tool call override decision`: 1 session (ccc5f6e)
- `Subagent surface routing (courier-only)`: 1 session (ccc5f6e)

All three sessions reported meaningful friction; two surfaced architectural concerns (bus surface, subagent sidecar cost). No repeats within the day, but patterns visible: communication fidelity (operator language, clarity), state isolation (worktree/main boundary, pane working dir), and charter/reality gaps (ask surface, sidecar assumptions).

## Signal — token usage

No `token_saver:` lines recorded in these interviews.

