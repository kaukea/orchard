session: skill-terseness-pass-landscaper · agent: landscaper (claude-opus-5) · date: 2026-07-29 · feature: skill-terseness-pass
deviations:
  - rule: sidecar edits must target the active worktree, never the main checkout · what: two Edit calls used the bare repo path instead of the worktree path, dirtying the main checkout's working tree with an uncommitted stray edit (caught immediately via git status/diff showing no change where one was expected; reverted with git checkout -- in the main checkout, confirmed nothing else was dirty there first, then redone at the correct path) · why: copied an absolute path without the worktree segment after an earlier correct Read at the worktree path
    change: tooling could warn or refuse an Edit/Write whose resolved path falls outside the active worktree root when one is in scope
  - rule: parallel sowers editing disjoint files should not share one mutable git index without isolation · what: dispatched 6 sowers into the same shared worktree without isolation:"worktree"; 3 of the resulting commits absorbed another sower's staged-but-uncommitted files under a commit message that did not mention them (verified byte-for-byte: no content lost, no two sowers touched the same file, only commit-message/diff attribution was messy — collapsed away at the close squash anyway) · why: assumed git add/commit isolation across concurrent Agent calls sharing one cwd, which the harness does not provide
    change: default parallel file-editing sower dispatch to isolation:"worktree" per sower, or serialize commits when sowers must share one working tree
token_saver: brief every sower up front to commit with an explicit pathspec and verify via 'git show --stat' before returning, rather than a bare 'git commit' — 2 of 6 sowers in this build discovered the shared-index race independently and spent tool calls diagnosing and fixing it after the fact; stating the rule in the step-spec once would have saved that overhead across all 6.
